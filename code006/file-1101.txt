<?php

// CreateManyItem

declare(strict_types=1);

namespace {{ tree.getFullNameSpaceOfFile(file, entity, action) }};

use App\Models\User;
{% for dependency in dependencyzz %}
use {{ dependency }};{% endfor %}

class {{ helper.getClassName(file, entity, action) }}
{
    /**
     * @param  array<int, array<string, mixed>>  $Mapzz
     * @return array<int, {{ helper.getClassName(helper.findFile('Model'), entity, action) }}>
     */
    static function run(int $UserId, array $Mapzz): array
    {
        /** @var array<int, {{ helper.getClassName(helper.findFile('Model'), entity, action) }}> $Itemzz */
        $Itemzz = [];
        foreach ($Mapzz as $Data) {
            $Item = new {{ helper.getClassName(helper.findFile('Model'), entity, 'CreateOne') }};
            $Item->fill($Data);
            {{ helper.getClassName(helper.findFile('CreateOne'), entity, 'CreateOne') }}::run($UserId, $Item, true);
            $Itemzz[] = $Item;
        }

        return $Itemzz;
    }

}
