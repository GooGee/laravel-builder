<?php

declare(strict_types=1);

namespace {{ tree.getFullNameSpaceOfFile(file, entity, action) }};

use Illuminate\Support\Facades\Log;

class {{ helper.getClassName(file, entity, action) }}
{
    public function __construct()
    {
    }

    function handle({{ helper.getClassName(helper.findFile('AdminEvent'), entity, action) }} $event)
    {
        Log::channel('admin')->info({{ helper.getClassName(helper.findFile('AdminEvent'), entity, action) }}::class, ['userId' => $event->user->id, 'id' => $event->item->id]);
    }
}
