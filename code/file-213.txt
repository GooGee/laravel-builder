<?php

declare(strict_types=1);

namespace {{ tree.getFullNameSpaceOfFile(file, entity, action) }};

use Illuminate\Support\Facades\Log;

class {{ tree.getClassName(file, entity, action) }}
{
    function handle({{ tree.getClassName(helper.findFile('Event'), entity, action) }} $event)
    {
        Log::info({{ tree.getClassName(helper.findFile('Event'), entity, action) }}::class, ['userId' => $event->user->id, 'id' => $event->item->id]);
    }
}
