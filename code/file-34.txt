<?php

declare(strict_types=1);

namespace {{ tree.getFullNameSpaceOfFile(file, schema, action) }};

use App\Api\AA\AbstractController;
{% for dependency in dependencyzz %}
use {{ dependency }};{% endfor %}
use Illuminate\Http\Request;

class {{ tree.getClassName(file, schema, action) }} extends AbstractController
{
    public function __invoke(Request $request, {{ action + schema.name }} $readMany)
    {
        $start = $request->input('start', PHP_INT_MAX);
        $start = $this->getPositiveIntegerOrNotFound($start);
        $itemzz = $readMany->readMany($start);

        return {{ action + schema.name }}Response::sendArray($itemzz->all());
    }
}
