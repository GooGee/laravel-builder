import { RichTextInput } from "ra-input-rich-text"
import {
    NumberInput,
    ReferenceInput,
    SelectInput,
    SimpleForm,
    TextField,
    TextInput,
} from "react-admin"

export default function {{ entity.name }}Form() {
    return (
        <SimpleForm>
            <TextField source="id" />

{% for item in columnzz %}
            <{{ item.raInput }} source="{{ item.alias }}" />
{% endfor %}
{% for reference in referencezz %}
            <ReferenceInput source="{{ reference[0] }}" reference="{{ reference[1] }}">
                <SelectInput optionText="name"></SelectInput>
            </ReferenceInput>
{% endfor %}
        </SimpleForm>
    )
}
