<?php

declare(strict_types=1);

namespace {{ tree.getFullNameSpaceOfFile(file, entity, action) }};

use App\Models\User;
{% for dependency in dependencyzz %}
use {{ dependency }};{% endfor %}

class {{ helper.getClassName(file, entity, action) }}
{
    public function __construct()
    {
    }

    /**
     * @param User $user
     * @param array<string, mixed> $data
     * @return \Illuminate\Contracts\Pagination\LengthAwarePaginator
     */
    function run(User $user, array $data)
    {
        $perPage = $data['pagination']['perPage'] ?? {{ helper.getClassName(helper.findFile('Model'), entity, action) }}::PageSize;
        $page = $data['pagination']['page'] ?? 1;

        $qb = {{ helper.getClassName(helper.findFile('Model'), entity, action) }}::query()
            ->where('userId', $user->id)
            ->whereNull('dtDelete');
        if (isset($data['sort']['field'])) {
            $qb->orderBy($data['sort']['field'], $data['sort']['order'] ?? "asc");
        }
        return $qb->paginate($perPage, ['*'], 'page', $page);
    }

}