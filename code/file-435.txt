<?php

declare(strict_types=1);

namespace {{ tree.getFullNameSpaceOfFile(file, entity, action) }};

{% for dependency in dependencyzz %}
use {{ dependency }};{% endfor %}
use Illuminate\Foundation\Auth\User;
use Tests\API\AbstractApiTest;

abstract class {{ tree.getClassName(file, entity, action) }} extends AbstractApiTest
{

    protected function path(): string
    {
        return "{{ entity.name }}";
    }

    /**
     * @param User $user
     * @param array<string, mixed> $attributes
     * @return {{ tree.getClassName(helper.findFile('Model'), entity, action) }}
     */
    static function makeItem(User $user, array $attributes = [])
    {
        $item = {{ tree.getClassName(helper.findFile('Model'), entity, action) }}::factory()
            ->for($user)
            ->make($attributes);
        return $item;
    }

    /**
     * @return string[]
     */
    static function structure(): array
    {
        return {{ tree.getClassName(helper.findFile('Model'), entity, action) }}::keyzz();
    }

}
