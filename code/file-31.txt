<?php

declare(strict_types=1);

namespace {{ tree.getFullNameSpaceOfFile(file, schema, action) }};

use App\Api\AA\AbstractController;
{% for dependency in dependencyzz %}
use {{ dependency }};{% endfor %}

class {{ tree.getClassName(file, schema, action) }} extends AbstractController
{
    public function __invoke(int $id,
                             {{ action + schema.name }}Request $request,
                             {{ action + schema.name }}        $update,
    )
    {
        $data = $request->validated();

        /** @var \{{ helper.getClassFullNameByFileSchema('Model', 'User') }} $user */
        $user = auth()->user();

        $item = $update($id,
            $user,
{% for column in columnzz %}
            $data['{{ column.name }}'],{% endfor %}
        );

        return {{ action + schema.name }}Response::sendItem($item);
    }
}
